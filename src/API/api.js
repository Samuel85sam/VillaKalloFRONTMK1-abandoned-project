import axios from 'axios';
//! ↑ lire doc d'axios!!!

export const GetAll = async () => {
    try {
        
        const listReponse = await;

    } catch (error) {
        
        console.log(error);
        return[];
    }
}